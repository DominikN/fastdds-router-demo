# TL;DR
# docker-compose -f docker-compose.talker.yml up

version: '2.3'

services:

  talker:
    image: ghcr.io/dominikn/ros-galactic-fastdds:v2.4.0
    environment:
      - RMW_IMPLEMENTATION=rmw_fastrtps_cpp
    command:
      - bash
      - -c
      - |
          source /fastdds_overlay/install/setup.bash
          ros2 run demo_nodes_cpp talker

  dds-router:
    # build: ./docker_image
    image: ghcr.io/dominikn/ros-galactic-fastdds:router
    network_mode: service:husarnet-dds-router
    volumes:
      - ./dds-router-config.yml:/config.yml
    command:
      - bash
      - -c
      - |
          source /fastdds_overlay/install/setup.bash
          /wait_for_discovery_server.sh
          /fastdds_overlay/install/ddsrouter/bin/ddsrouter -c /config.yml -r 2

  husarnet-dds-router:
    image: husarnet/husarnet
    volumes:
      - /var/lib/husarnet  # This will persist your Husarnet Client keys, thus IP of the container will be stable/the same between (re)boots
    sysctls:
      - net.ipv6.conf.all.disable_ipv6=0  # Husarnet is using IPv6 for the internal connections
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun
    environment:
      - HOSTNAME=talker
    env_file:
      - ./.env  # create .env file in the same folder
